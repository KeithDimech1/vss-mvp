import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // basePath temporarily disabled to fix routing issues
  // Can re-enable later once site is working
  // basePath: "/vss/lithodat",
};

export default nextConfig;
